[project]
name = "mcp-notes-server"
version = "0.1.0"
description = "MCP Notes server (stdio) — навчальний приклад для lesson-16.MCP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
]

[tool.uv]
package = false
